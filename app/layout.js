export const metadata = { title: 'Alma.+', description: 'Trouvez vos clients automatiquement' }
export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  )
}
